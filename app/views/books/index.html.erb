<% provide(:title, 'All Books') %>
<h1>All books</h1>

<%= will_paginate %>

<ul class="books">
  <% @users.each do |user| %>
    <li>      
      <%= link_to book.title, user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>