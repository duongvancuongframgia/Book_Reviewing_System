<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
  html: { method: :delete },
  remote: true) do |f| %>
  <%= f.submit t("views.shared.unfollow.unfollow_bt"), class: "btn" %>
<% end %>
